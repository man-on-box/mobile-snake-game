{
  "name": "my-snake-game",
  "version": "1.0.0",
  "description": "My Snake Game",
  "repository": "github:man-on-box/mobile-snake-game",
  "main": "index.ts",
  "scripts": {
    "dev": "npm run tscheck && npm run clean && parcel src/index.html",
    "build": "npm run tscheck && npm run clean && parcel build src/index.html",
    "tscheck": "tsc --noEmit",
    "clean": "rm -rf ./.cache && rm -rf ./dist"
  },
  "keywords": [
    "snake",
    "game"
  ],
  "author": "man-on-box",
  "license": "ISC",
  "devDependencies": {
    "@types/is-touch-device": "^1.0.0",
    "autoprefixer": "^9.8.6",
    "parcel-bundler": "^1.12.4",
    "postcss-modules": "^3.2.0",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "is-touch-device": "^1.0.1"
  }
}
